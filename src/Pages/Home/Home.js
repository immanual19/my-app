import React from 'react';
import Login from '../Authentication/Login/Login';

const Home = () => {
    return (
        <div>
            <Login></Login>
        </div>
    );
};

export default Home;